<Window x:Class="FaceDetection.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FaceDetection.Views"
        xmlns:vm="clr-namespace:FaceDetection.ViewModels"
        xmlns:conv="clr-namespace:FaceDetection.Views.Converters"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525">
    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>
    
    <Grid>
        <Grid.DataContext>
            <vm:DetectionViewModel />
        </Grid.DataContext>
        
        <Image HorizontalAlignment="Left" Height="266" Margin="10,10,0,0" VerticalAlignment="Top" Width="497" Source="{Binding Path=OutputImage, Converter={StaticResource bitmapConverter}}" />
        
        
    </Grid>
</Window>
