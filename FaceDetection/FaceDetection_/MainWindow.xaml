<Window x:Class="FrameDetection.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:FaceDetection.ViewModels"
        xmlns:conv="clr-namespace:FaceDetection.Views.Converters"
        xmlns:local="clr-namespace:FaceDetection"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525">
    <Window.DataContext>
        <vm:MainViewModel />
    </Window.DataContext>
    
    <Window.Resources>
        <ResourceDictionary>
            <conv:ImageConverter x:Key="imageConverter"/>
        </ResourceDictionary>
    </Window.Resources>
    
    <Grid>
        <Image x:Name="image" HorizontalAlignment="Left" Height="300" Margin="10,10,0,0" VerticalAlignment="Top" Width="497" Source="{Binding Path=Image}"/>
    </Grid>
</Window>
